apply plugin: 'scala'
apply plugin: 'idea'

repositories {
    jcenter()
}

dependencies {
    compile 'org.scala-lang:scala-library:2.12.3'
}

idea {
	project {
		vcs = 'Git'
	}
	module {
		downloadSources = true
	}
}
